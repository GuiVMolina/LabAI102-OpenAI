# 📦 Instalação das Dependências do Projeto

Para configurar o ambiente de desenvolvimento e instalar todas as bibliotecas necessárias para este projeto (OpenAI API e gerenciamento de variáveis de ambiente), execute o comando abaixo no seu terminal:

```bash
pip install openai python-dotenv
